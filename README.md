# Wealth with Ayush

## Development

```sh
npm i
npm run dev
```

The application is a TanStack Start site. Run these commands from this directory.

## Razorpay setup

Copy `.env.example` to `.env` and add the Razorpay test or live credentials before using checkout. The public key is exposed to the browser; the secret remains server-side.
